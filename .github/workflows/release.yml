name: Release

on:
  push:
    tags: ['v*.*.*']
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g., v1.0.0)'
        required: true
        type: string

permissions:
  contents: write
  packages: write
  security-events: write
  attestations: write
  id-token: write

jobs:
  release:
    uses: tesserix/tesserix-infra/.github/workflows/_release-nextjs.yml@main
    with:
      service_name: tesserix-home
      service_title: Tesserix Home
    secrets:
      PKG_READ_TOKEN: ${{ secrets.PKG_READ_TOKEN }}
      GCP_WORKLOAD_IDENTITY_PROVIDER: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
      GCP_SERVICE_ACCOUNT: ${{ secrets.GCP_SERVICE_ACCOUNT }}

  deploy:
    needs: [release]
    uses: tesserix/tesserix-infra/.github/workflows/_deploy-cloudrun.yml@main
    with:
      service_name: tesserix-home
      image: ${{ needs.release.outputs.image }}
      health_path: /api/health
    secrets:
      GCP_WORKLOAD_IDENTITY_PROVIDER: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
      GCP_SERVICE_ACCOUNT: ${{ secrets.GCP_SERVICE_ACCOUNT }}
      DISPATCH_TOKEN: ${{ secrets.DISPATCH_TOKEN }}
