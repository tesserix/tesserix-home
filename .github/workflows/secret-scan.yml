name: Secret scan

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  scan:
    name: Secrets
    uses: tesserix/tesserix-workflows/.github/workflows/secret-scan.yml@v2.1.0
