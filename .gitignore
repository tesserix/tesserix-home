# Dependencies
node_modules/
.pnp/
.pnp.js

# Build outputs
.next/
out/
dist/
build/

# Environment files
.env
.env.local
.env.dev
.env.production
.env*.local

# IDE
.idea/
.vscode/
*.swp
*.swo
*.code-workspace

# OS
.DS_Store
Thumbs.db

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/

# Playwright E2E Tests
.playwright/
playwright/.cache/
**/test-results/
**/playwright-report/
playwright/blob-report/
playwright/html-report/
playwright/.auth/

# TypeScript
*.tsbuildinfo
*.d.ts.map
next-env.d.ts

# Misc
*.pem
.vercel

# Secrets
**/secret.json
**/secret.yaml
**/secrets.json
**/secrets.yaml

# Playwright MCP
.playwright-mcp/

# Images (prevent accidental large asset commits)
**.png
# Allow public assets (web app moved to apps/web/ in the monorepo restructure)
!apps/web/public/**/*.png
# Allow mobile app assets (icon/splash are EAS build inputs — must be committed)
!apps/mobile/assets/**/*.png
# Allow console assets. `app/*.png` covers Next's file-based metadata
# convention (icon.png, apple-icon.png) which must sit beside the routes, not
# in public/. Without these two lines the blanket **.png rule drops them
# silently: the build succeeds and production serves a broken logo.
!apps/console/public/**/*.png
!apps/console/app/*.png

.claude/

# Monorepo workspaces
**/node_modules
apps/*/.next
apps/*/.expo
packages/*/dist
.turbo
**/*.tsbuildinfo

# Superpowers SDD scratch
.superpowers/
