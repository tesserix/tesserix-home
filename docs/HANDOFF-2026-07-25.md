# Session Handoff — 2026-07-25

## Current state: everything merged + deployed
`main` = `6773ad9`. Prod (`tesserix` ns, `company` deploy) runs **`main-6773ad9`, 1/1 Ready, Kargo promotion Succeeded**. Contains, in order:
- **PR #47** — pnpm + Turborepo monorepo restructure (`apps/web` Next.js, `apps/mobile` Expo, `packages/*`).
- **PR #52** — `format.ts` extracted into shared `@tesserix/homechef-shared` (tsup; consumed by both Next + Metro).
- **PR #54** — HomeChef admin `contracts.ts` extracted into the same package (canonical = web superset; mobile adopted web shapes, proven safe by mobile typecheck).
- **PR #51** — mobile backend auth fixes: `getCurrentSession` bearer fallback (fixes reads 401) + CSRF bearer exemption (fixes writes 403).

No open PRs remain. Nothing in flight.

## Key facts (also in memory)
- **CI footgun FIXED**: `ci.yml` build/push gated to `if: github.ref == 'refs/heads/main'` — feat branch pushes no longer publish images / auto-deploy. Validate branches via PRs (pull_request skips build/push).
- **Kargo prod promotion is RBAC-gated**: kubectl can't create Promotions; use `kargo login` + `kargo promote` or the Kargo UI. Merging to `main` auto-deploys (warehouse polls 5m, autoPromotion on).
- **Argo `company` app**: `health=Progressing` forever is BENIGN — the dead `kong` Ingress (no kong controller; traffic is via istio `company-vs` VirtualService, TLS at Cloudflare). Do NOT "fix" it by removing the Ingress (`prune: false` would orphan it → OutOfSync). Promotions key on Synced, not full health.
- Infra lives in sibling repo `../tesserix-k8s` (GitOps → Argo). Fixed there this session: de-duped `default-deny-ingress` + `public-endpoints-tesserix` (were co-owned by `company` + `istio-config` → chronic OutOfSync).

## What's NOT done (next steps)
1. **Mobile app real login** — code is deployed but needs GCP config to actually sign in: create iOS + Web Google OAuth clients for `app.tesserix.admin`, set backend `GOOGLE_MOBILE_CLIENT_IDS` (+ `SESSION_ENCRYPT_KEY`, `ALLOWED_ADMIN_EMAILS`), set `EXPO_PUBLIC_GOOGLE_*`, and add the iOS reversed-client-id URL scheme to `apps/mobile/app.json`. Full runbook: `docs/mobile-admin-runbook.md`. This is operator/GCP work.
2. **De-dup follow-ups (deferred)**: `HttpClient` port + api transports and `auth` stay per-app (platform-specific: web fetch+cookie vs mobile axios+bearer). Plans/sketch under `docs/superpowers/`.
3. **Minor follow-up** (from #51 review): `apps/web/app/api/auth/mobile/session/route.ts` still parses the bearer inline — could use the shared `bearerToken` helper (`apps/web/lib/auth/bearer.ts`). Pre-existing, behaviorally equivalent.
4. **Mobile Platform tab** is intentional "Soon" stubs; the HomeChef Go `/api/v1/admin/*` endpoints live in a separate repo.

## How to verify prod anytime
```
kubectl -n tesserix get deploy company -o jsonpath='{.spec.template.spec.containers[*].image} {.status.readyReplicas}/{.status.replicas}'
kubectl -n kargo-tesserix-home get promotions --sort-by=.metadata.creationTimestamp | tail -2
```
